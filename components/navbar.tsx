import { FC } from 'react';
import ThemeSwitch from './theme-switch';

const Navbar: FC = () => {
  return (
    <div>
      <div>Nav</div>
      <ThemeSwitch />
    </div>
  );
};

export default Navbar;
